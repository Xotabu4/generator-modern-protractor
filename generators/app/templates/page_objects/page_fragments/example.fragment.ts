import { BaseFragment } from "protractor-element-extend";

export class ExampleFragment extends BaseFragment {
  // https://github.com/Xotabu4/protractor-element-extend
  // Use this structure to define own fragments on the page
}
